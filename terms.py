

def get_partitions(n):
  
  
    if n == 0: #базовый случай: число равно 0, разбиение - пустой список
        return [[]]  
    
    result = [] #инициализируем список для хранения разбиений
    for i in range(1, n + 1): #перебираем все возможные значения первого элемента разбиения от 1 до n
        for partition in get_partitions(n - i): #рекурсивно вызываем функцию для оставшейся части числа (n-i)
            result.append([i] + partition) #добавляем текущее значение i к началу каждого полученного разбиения
    return result #возвращаем список всех найденных разбиений
        k = int(input("Введите целое число больше или равное 0: ")) #запрашиваем ввод числа от пользователя
          partitions = get_partitions(k) #вызываем функцию для нахождения разбиений
          print(f"Все возможные разбиения числа {k}:") #выводим результат
          for partition in partitions:
              print(partition) #выводим каждое разбиение на отдельной строке
 
